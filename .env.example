# Copier vers .env et remplir avec les vraies valeurs (Settings → API).
# Ne jamais committer .env.
#
# Sur Vercel : Settings → Environment Variables (Production)
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY
# Puis Redeploy. Les variables VITE_* doivent être présentes au BUILD.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
