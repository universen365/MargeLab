# Copier vers .env et remplir (ne jamais committer .env).
#
# Supabase (Vercel → Environment Variables + Redeploy) :
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Login en dur (optionnel — sinon valeurs par défaut dans le code) :
# VITE_LOGIN_USERNAME=margelab
# VITE_LOGIN_PASSWORD=margelab
# VITE_LOGIN_EMAIL=margelab@app.local
